var PartRepository = function (partFactory, partCollection) {
    this.partFactory = partFactory;
    this.partCollection = partCollection;
};

PartRepository.prototype.getPart = function () {
    //TODO
};

module.exports = PartRepository;
